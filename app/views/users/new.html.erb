<% provide(:title, '新規登録') %>
<% provide(:button_text, 'アカウント作成') %>
<h1>アカウント作成</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form', url: signup_path %>
  </div>
</div>
